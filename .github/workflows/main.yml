on:
  repository_dispatch
  
jobs:
  verify_and_codesign:
    runs-on: ubuntu-latest
    env:
      S3_BUCKET: "mveytsman-test"
    steps:
        - run: echo $MESSAGE
          env:
             MESSAGE: ${{ github.event.client_payload.path }}
